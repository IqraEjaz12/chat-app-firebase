import 'package:get/get.dart';

class WelcomeState {
  // Observable variable to track the current index
  // This is typically used in a PageView or TabBar to manage the current page
  // It allows the UI to reactively update when the index changes
  var index = 0.obs;
}
